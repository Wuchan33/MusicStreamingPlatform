# Copy to .env before the first run:  cp .env.example .env
# Local development only - the container is disposable and bound to localhost.

POSTGRES_USER=music
POSTGRES_PASSWORD=postgres
POSTGRES_DB=musicstreaming
